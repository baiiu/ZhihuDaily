package baiiu.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import baiiu.greendao.gen.SavedDailyDetail;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SAVED_DAILY_DETAIL".
*/
public class SavedDailyDetailDao extends AbstractDao<SavedDailyDetail, Long> {

    public static final String TABLENAME = "SAVED_DAILY_DETAIL";

    /**
     * Properties of entity SavedDailyDetail.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Body = new Property(1, String.class, "body", false, "BODY");
        public final static Property Image_source = new Property(2, String.class, "image_source", false, "IMAGE_SOURCE");
        public final static Property Image = new Property(3, String.class, "image", false, "IMAGE");
        public final static Property Title = new Property(4, String.class, "title", false, "TITLE");
        public final static Property Share_url = new Property(5, String.class, "share_url", false, "SHARE_URL");
        public final static Property Js = new Property(6, String.class, "js", false, "JS");
        public final static Property Css = new Property(7, String.class, "css", false, "CSS");
    };


    public SavedDailyDetailDao(DaoConfig config) {
        super(config);
    }
    
    public SavedDailyDetailDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SAVED_DAILY_DETAIL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"BODY\" TEXT," + // 1: body
                "\"IMAGE_SOURCE\" TEXT," + // 2: image_source
                "\"IMAGE\" TEXT," + // 3: image
                "\"TITLE\" TEXT," + // 4: title
                "\"SHARE_URL\" TEXT," + // 5: share_url
                "\"JS\" TEXT," + // 6: js
                "\"CSS\" TEXT);"); // 7: css
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SAVED_DAILY_DETAIL\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SavedDailyDetail entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String body = entity.getBody();
        if (body != null) {
            stmt.bindString(2, body);
        }
 
        String image_source = entity.getImage_source();
        if (image_source != null) {
            stmt.bindString(3, image_source);
        }
 
        String image = entity.getImage();
        if (image != null) {
            stmt.bindString(4, image);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(5, title);
        }
 
        String share_url = entity.getShare_url();
        if (share_url != null) {
            stmt.bindString(6, share_url);
        }
 
        String js = entity.getJs();
        if (js != null) {
            stmt.bindString(7, js);
        }
 
        String css = entity.getCss();
        if (css != null) {
            stmt.bindString(8, css);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SavedDailyDetail readEntity(Cursor cursor, int offset) {
        SavedDailyDetail entity = new SavedDailyDetail( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // body
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // image_source
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // image
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // title
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // share_url
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // js
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // css
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SavedDailyDetail entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setBody(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setImage_source(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setImage(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTitle(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setShare_url(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setJs(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setCss(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SavedDailyDetail entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SavedDailyDetail entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
